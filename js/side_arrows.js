// Получаем кнопки для переключения слайдов
const prevButton = document.getElementById('prevSlide');
const nextButton = document.getElementById('nextSlide');

// Обработчики событий для кнопок "Previous" и "Next"
prevButton.addEventListener('click', () => {
    goToPreviousSlide(); // Переключаемся на предыдущий слайд
});

nextButton.addEventListener('click', () => {
    goToNextSlide(); // Переключаемся на следующий слайд
});

// Функция для переключения на предыдущий слайд
function goToPreviousSlide() {
    // Ваш код для переключения на предыдущий слайд
    // Например, можно использовать текущий индекс слайда и изменить его
}

// Функция для переключения на следующий слайд
function goToNextSlide() {
    // Ваш код для переключения на следующий слайд
    // Например, можно использовать текущий индекс слайда и изменить его
}
